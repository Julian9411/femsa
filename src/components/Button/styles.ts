import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttons: {
    borderRadius: 10,
    alignItems: 'center',
    width: '30%',
    backgroundColor: '#ABE8FF',
  },
  textColor: {
    color: 'black',
  },
});
